let string1 = 'Polar Bear';
let string2 = 'Polar bear';
let string3 = 'Penguin';

// You could convert each string to either lowercase or uppercase
// to make the character case irrelevant when checking equality 

string1.toUpperCase() === string2.toUpperCase(); // true
string1.toLowerCase() === string2.toLowerCase(); // true