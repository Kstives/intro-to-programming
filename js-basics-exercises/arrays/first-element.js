function first(array) {
  return array[0];
}

console.log(first(['Earth', 'Moon', 'Mars'])); 

// if the input array was empty, it would return undefined

// arrow function implementation

first2 = (array) => array[0];

console.log(first2(['Earth', 'Moon', 'Mars']));

