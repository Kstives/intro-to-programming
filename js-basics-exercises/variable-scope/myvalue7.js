let a = 1;

function myFunction() {
  console.log(a);
}

myFunction(); // This logs 1 to the console
// variable a is declared with a global scope, so it is also accessable within 
// block statements, such as myFunction above.