console.log(greeting);

var greeting = 'Hello world!';

// This will log 'Hello World' because greeting's scope is global 
// and it doesn't matter in what order a variable is initialized and used.

// Actually, it logged undefined. The order does matter.
